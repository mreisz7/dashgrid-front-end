import React, { Component } from 'react';
import './scss/linkCard.scss';

interface LinkCardProps {
    performLinkClick: () => void;
}

class App extends Component<LinkCardProps, {}> {
    render() {
        return (
            <div></div>
        )
    }
}